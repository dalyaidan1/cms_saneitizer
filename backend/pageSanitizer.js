function pageSanitizer(page){

    // basic sanitization
    // clean tags
    // clean attributes/props
    // remove ads
    
    // organize to template

    // add to db --
    //  just return a page for now to get mvp out with a json

}

export default (page) => pageSanitizer(page)